# Plan

- One root-module draft for Cleric-only preparation counts, domain grants,
  deferred Light, and creation/advancement regressions.
- Validate native Cleric finalization/reload and all valid Bard four-spell choices.
- API draft adopts a pushed provider commit; update native fixtures and exercise
  creation, private reads, spell offers and Bard isolation using real handlers.
- Build local stack and verify the browser preparation picker before handoff.
- No automatic merges. Replace API pseudo pin with provider release after the
  user merges the toolkit draft. No workspace files or local replacements.
