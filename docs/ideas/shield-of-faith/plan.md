# Implementation plan

- Root: condition, factory/loader/display registration, cast profile and native
  Cleric acquisition. Prove AC, nonstacking, reload, cleanup and cost metadata.
- API: adopt pushed root commit in a draft, test real handlers, authenticated
  sheet AC, replay, concentration removal and casting economy in both orders.
- Build local image against the draft pin, retain saved play data, and verify
  native browser acquisition/casting before user acceptance.
- Keep PRs draft until tested. Merge only on user instruction; after root release,
  replace the API pseudo-version with the release tag. No local replacements.

Current baseline: toolkit main c45fe571; API dev fb7ed78. No overlapping
Shield of Faith PR found. Proto and web changes only if acceptance proves a gap.
