# Plan

Add finalization and JSON round-trip coverage, verify martial attack bonuses,
and run the full root module tests and lint. Publish a draft, adopt its pushed
commit in API, and verify native creation and owner-sheet persistence. No merges
without user acceptance; release pins replace development pins before merge.
